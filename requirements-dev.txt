# Development dependencies for Metadata-to-Morphsource-compare
# Install with: pip install -r requirements-dev.txt

# Include production requirements
-r requirements.txt
-r requirements-test.txt

# Code formatting
black>=23.0.0
isort>=5.12.0

# Linting
flake8>=6.0.0
flake8-docstrings>=1.7.0
pylint>=3.0.0

# Ruff: fast, single-binary linter + formatter used by the CI workflow.
# Mirrors the .flake8 rule set (see pyproject.toml `[tool.ruff]`).
ruff>=0.6.0

# YAML linting (CI uses yamllint for .github/workflows/)
yamllint>=1.35.0

# Dependency-CVE scanner (CI runs `pip-audit` in advisory mode)
pip-audit>=2.7.0

# Type checking
mypy>=1.0.0

# Security checking
bandit>=1.7.0
safety>=2.3.0

# Pre-commit hooks
pre-commit>=3.0.0

# Build tools
build>=1.0.0
twine>=4.0.0

# Documentation (optional)
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0

# Jupyter for interactive development
jupyter>=1.0.0
ipython>=8.0.0
